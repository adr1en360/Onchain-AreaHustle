{
  "name": "onchain-areahustle",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network celoSepolia",
    "deploy:celo": "hardhat run scripts/deploy.ts --network celo",
    "sync:env": "npx ts-node scripts/sync-env.ts",
    "check:deployer": "npx ts-node scripts/check-deployer.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "@openzeppelin/contracts": "^5.4.0",
    "@types/node": "^22.16.5",
    "dotenv": "^17.4.2",
    "hardhat": "^2.28.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
